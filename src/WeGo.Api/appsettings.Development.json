{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "$comment": [
    "The production rate limits exist to stop a stranger filling the disk.",
    "Applied to a machine running the app for one developer they only get in the",
    "way: the UX audit seeds three trips per run and was refused on its second",
    "run of the hour, which reads as a broken harness rather than a working",
    "limit. Never deployed — the Fly machine runs Production."
  ],
  "RateLimits": {
    "JoinPerMinute": 100000,
    "GlobalPerMinute": 1000000,
    "CreateTripPerHour": 100000,
    "GeocodePerMinute": 100000
  }
}
